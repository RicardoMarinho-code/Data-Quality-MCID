# Nome do arquivo da planilha do governo a ser avaliada.
# Copie este arquivo para ".env" e ajuste o valor conforme necessario.
ARQUIVO_DADOS=Amostra de Dados.csv

# Caminho do relatorio de saida (opcional).
ARQUIVO_RELATORIO=relatorio_qualidade.csv

# Tolerancia, em R$, para a checagem de consistencia do valor total (opcional).
TOLERANCIA_VALOR=1.0
